- [RBAC routing layer](rbac-routing.md) — RestaurantDashboard.jsx is the role-based routing layer for /:restaurantSlug/:pageSlug; delegates to AdminDashboard after role enforcement.
- [DashboardApp pageSlug param](dashboard-pageslug.md) — static routes like /:slug/profile don't expose :pageSlug; must use a single dynamic /:slug/:pageSlug route instead.
- [Replit subdomain routing fix](replit-subdomain.md) — On Replit/localhost, getSubdomain() must return null (not "dashboard") so DefaultApp loads with the full route tree including /dashboard, /auth, etc.
- [Menu system RLS fix](menu-rls-fix.md) — anon key reads of menu_categories/menu_items are blocked by RLS; fixed by routing all reads through server-side API (service role key, bypasses RLS entirely).
- [Replit migration setup](replit-migration.md) — Supabase stays as-is (auth + data + realtime); Replit PostgreSQL (DATABASE_URL) used for per-restaurant schemas; VITE_DISABLE_AUTH=true is dev-only via [userenv.development] in .replit.
- [Neon shadow-write pattern](neon-shadow-write.md) — Phase C: non-blocking shadow-writes to Neon after Supabase restaurant create/update. db.js uses fetch; server.js/vite.config.js call patchNeonRestaurant() directly. Neon sql.query() returns rows array directly, not {rows:[]}.
- [Menu category shadow-write](menu-category-shadow-write.md) — E1A: upsert/delete shadow-writes for menu_categories added to server.js + vite.config.js mutation routes; reads still go to Supabase. position coerced ?? 0; uses ON CONFLICT (id) DO UPDATE.
- [Menu item shadow-write](menu-item-shadow-write.md) — E2A+E2B: image TEXT compat column added to Neon (Drizzle migration 0002); shadow-writes in neon-menu-items.js. FK silently fails for Supabase-only restaurants (correct behavior). VITE_SUPABASE_URL includes /rest/v1/ suffix — strip with regex before direct Supabase REST calls.
- [Orders shadow-write pattern](orders-shadow-write.md) — Phase G: migration 0003 adds items JSONB to Neon orders. upsertNeonOrder sets order_number=id (NOT NULL). restaurant_id casts ::uuid. POST /api/orders handles Supabase+Neon; GET is Neon-first. node -e doesn't support top-level await; use --input-type=module piped instead.
- [Team members migration split](team-members-migration.md) — Phase H1: writes stay client-side Supabase (RLS + auth.uid for owner_id). Shadow-write via /api/team-members/shadow-upsert|shadow-delete after Supabase confirms. link_team_member_on_login + get_my_restaurant_ids RPCs must stay in Supabase — they are the auth/access gate.
- [Better Auth migration](better-auth-migration.md) — Supabase Auth replaced with Better Auth v1.6.23. auth.server.js+auth-client.js+api/auth.js+api/auth-check.js. db.js uses current-user.js module store instead of supabase.auth.getUser(). Vercel: /api/auth/:path* → /api/auth?_path=:path*. Prod needs BETTER_AUTH_SECRET + SUPERADMIN_ALLOWED_EMAILS + BETTER_AUTH_BASE_URL + DATABASE_URL (Supabase direct) in Vercel. Better Auth DB tables created via raw SQL (no CLI migration command in v1.6.23). crossSubDomainCookies: {domain: '.exzibo.online'} for shared session across subdomains.

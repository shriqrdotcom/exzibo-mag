You are my Replit AI backend migration engineer.

I reviewed the Phase D2 analysis.

Do NOT fully switch "getRestaurants()" yet.

Proceed with D2A only: prepare restaurant list migration safely.

Important:
Do NOT remove Supabase.
Do NOT remove Supabase env variables.
Do NOT change Auth.
Do NOT add Better Auth.
Do NOT change Realtime.
Do NOT add Ably.
Do NOT change Storage.
Do NOT add Cloudflare R2.
Do NOT add Upstash.
Do NOT change UI/design.
Do NOT move menu, orders, bookings, staff, or notifications.
Do NOT switch dashboard list reads fully yet.
Do NOT modify deleted restaurant page, MasterControl, or unrelated pages.

Goal:
Fix the required prerequisites before switching "getRestaurants()" to Neon-first.

Scope for D2A:

1. Add Neon shadow-write for soft delete.
2. Add/verify a Neon restaurant list API route for future D2.
3. Normalize the "tables" field from "table_numbers.length".
4. Keep current "getRestaurants()" live behavior unchanged unless only adding safe fallback helpers.

Tasks:

1. Inspect "softDeleteRestaurant(id)" in "src/lib/db.js".

2. After Supabase soft-delete succeeds, add a non-blocking Neon shadow-update:
   
   - PATCH "/api/neon/restaurant/:id"
   - payload:
     {
     "is_deleted": true,
     "deleted_at": current timestamp
     }

3. Supabase must remain authoritative:
   
   - If Supabase soft-delete fails, return the same error as before.
   - If Supabase succeeds but Neon fails, do not break the app.
   - Log Neon failure safely with "console.warn".
   - Do not throw Neon errors to the frontend.

4. Add or verify Neon list API route:
   
   - GET "/api/neon/restaurants"
   - It should support optional "ids" query parameter for future authenticated filtering.
   - It must filter "is_deleted = false" by default.
   - It must order by "created_at desc".
   - It must use Drizzle/Neon only.
   - It must not replace current Supabase list reads yet.

5. Add response shape compatibility for Neon restaurant rows:
   
   - Keep all existing restaurant fields.
   - Add computed "tables" field:
     - if "table_numbers" is an array and length > 0, return String(table_numbers.length)
     - otherwise return null
   - Do not add a real "tables" column to Neon unless absolutely necessary.

6. Keep Supabase RPC "get_my_restaurant_ids" unchanged.
   It is still required for auth/team-member permissions until Better Auth and role migration happen later.

7. Tests:
   
   - Soft-delete a test restaurant through existing flow.
   - Confirm Supabase row has "is_deleted = true".
   - Confirm Neon shadow row has "is_deleted = true" and "deleted_at".
   - Confirm deleted restaurant does not appear from Neon list route.
   - Confirm current app still works.
   - Confirm "getRestaurants()" still uses Supabase for now.
   - Confirm menu/orders/bookings/staff/auth/realtime/storage untouched.

8. Report:
   
   - files changed
   - functions changed
   - Neon route created/verified
   - soft-delete sync result
   - tables normalization result
   - confirmation current dashboard list still reads from Supabase
   - whether full D2 implementation is safe next

Final rule:
D2A only prepares the restaurant list migration. Do not fully switch "getRestaurants()" yet.
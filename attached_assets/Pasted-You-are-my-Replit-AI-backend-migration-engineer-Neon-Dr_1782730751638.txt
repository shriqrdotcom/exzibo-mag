You are my Replit AI backend migration engineer.

Neon + Drizzle foundation is now complete:

- DATABASE_URL points to the correct Neon database
- Drizzle migration is applied
- 11 Exzibo tables exist in Neon
- /api/health/neon works
- Supabase is still untouched

Now I want to prepare the next migration step carefully.

Goal:
Analyze ONLY the restaurant profile / restaurant creation data flow before moving it to Neon + Drizzle.

Important:
Do NOT change code yet.
Do NOT create new API routes yet.
Do NOT modify existing API routes yet.
Do NOT remove Supabase.
Do NOT remove Supabase environment variables.
Do NOT replace any Supabase code yet.
Do NOT change Auth.
Do NOT add Better Auth.
Do NOT change Realtime.
Do NOT add Ably.
Do NOT change Storage.
Do NOT add Cloudflare R2.
Do NOT add Upstash.
Do NOT migrate old Supabase data.
Do NOT change frontend UI/design.
Do NOT move menu items, orders, bookings, staff, notifications, or images.

This analysis is ONLY for:

- restaurants table
- restaurant_settings table if directly used in restaurant profile
- table_numbers table only if directly used in restaurant profile

Tasks:

1. Inspect the existing restaurant creation/profile flow.

2. Find every file that uses restaurant profile data.

3. Find every "supabase.from("restaurants")" usage.

4. Find every "supabase.from("restaurant_settings")" usage if it exists.

5. Find every "supabase.from("table_numbers")" usage if it is connected to restaurant profile.

6. Find all API routes related to:
   
   - restaurant creation
   - restaurant fetch
   - restaurant update
   - restaurant profile settings
   - restaurant slug or UID lookup

7. Find all frontend pages/components that depend on restaurant profile data.

8. List every restaurant field currently used, for example:
   
   - id
   - name
   - slug
   - uid
   - logo
   - phone
   - address/location/place
   - plan
   - status
   - currency
   - accent_color
   - table_numbers
   - social_links
   - opening hours
   - images
   - settings

9. Identify which fields already exist in the new Neon + Drizzle schema and which fields are missing or need mapping.

10. Identify the safest API route plan for moving restaurant profile to Neon + Drizzle.

11. Identify what must stay on Supabase for now.

12. Identify any risk that can break the app if restaurant profile is moved incorrectly.

Report format:

1. Current restaurant profile data flow
2. Files using restaurant profile data
3. Supabase tables used
4. Fields used
5. Existing API routes
6. Frontend dependencies
7. Neon schema field mapping
8. Missing fields or schema changes needed
9. Recommended implementation plan
10. Risks/warnings
11. Exact files that will need changes in the next step

Final rule:
This is analysis only. Stop after the report and wait for my approval before making any code changes.
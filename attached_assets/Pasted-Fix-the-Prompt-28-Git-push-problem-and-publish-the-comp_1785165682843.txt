Fix the Prompt 28 Git push problem and publish the completed work on a guaranteed-new GitHub branch.

Do not restart Prompt 28.
Do not modify the completed CI implementation.
Do not push, pull, rebase, or force-push any previous Prompt 28 branch.
Do not use the Replit Git-panel Push button.

First preserve the completed work.

1. Check the current state:

git status
git branch --show-current
git rev-parse HEAD
git log --oneline -5

2. Review all uncommitted files:

git diff --stat
git diff --name-only

Only Prompt 28 files are allowed, including:

- .github/workflows/ci.yml
- CI tests or validation scripts
- narrow package scripts
- narrow Node 22 configuration

Exclude:

- screenshots
- attached_assets
- pasted prompts
- Agent-memory files
- .env files
- secrets
- generated output
- unrelated source changes

3. If intended changes are uncommitted, stage only those reviewed files and commit:

git add .github/workflows/ci.yml

Add any other intended CI files individually by exact path.

git commit -m "fix: restore production ci quality gates"

Do not create an empty commit.

4. Save the completed commit SHA:

COMPLETED_SHA=$(git rev-parse HEAD)
echo "$COMPLETED_SHA"

5. Fetch GitHub without changing the completed commit:

git fetch origin --prune

6. Generate a completely unique branch name using the current time and commit SHA:

NEW_BRANCH="repair/28-ci-gates-$(date +%Y%m%d-%H%M%S)-$(git rev-parse --short HEAD)"
echo "$NEW_BRANCH"

7. Confirm the branch does not exist remotely:

git ls-remote --exit-code --heads origin "$NEW_BRANCH"

If this command finds an existing branch, generate another name with a new timestamp. Do not reuse an existing branch.

8. Create the new branch directly from the completed commit:

git switch -c "$NEW_BRANCH" "$COMPLETED_SHA"

Confirm:

git branch --show-current
git rev-parse HEAD

The current SHA must equal COMPLETED_SHA.

9. Push using an explicit new remote reference:

git push --set-upstream origin HEAD:refs/heads/"$NEW_BRANCH"

Do not use:
- git push --force
- git push --force-with-lease
- git pull
- git pull --rebase
- any previous Prompt 28 branch name

10. Capture and show the complete Git push output.

If GitHub rejects the workflow file because the connection lacks workflow permission, stop and report:

BLOCKED: GitHub authorization cannot create or update .github/workflows/ci.yml.

Do not repeatedly retry.
Do not remove the CI workflow to make the push succeed.

11. Verify the new remote branch:

LOCAL_SHA=$(git rev-parse HEAD)
REMOTE_SHA=$(git ls-remote origin "refs/heads/$NEW_BRANCH" | cut -f1)

echo "Local: $LOCAL_SHA"
echo "Remote: $REMOTE_SHA"

The two SHAs must match.

12. Create a pull request:

Base:
main

Compare:
the exact value of NEW_BRANCH

Title:
fix: restore production ci quality gates

Description:

Restore mandatory production CI quality gates.

Changes:
- Added GitHub Actions validation for pull requests and pushes to main.
- Enforced Node.js 22 and frozen dependency installation.
- Added disposable PostgreSQL setup with migrations before database tests.
- Added migration, security, regression, production-build, Vercel function-count, Worker TypeScript, and Wrangler dry-run gates.
- Added least-privilege workflow permissions.
- No production resource or secret was accessed.

Do not merge the pull request.
Do not delete the branch.

Return exactly:

PROMPT 28 PUSH RESULT

Completed commit SHA:
New branch:
Local SHA:
Remote SHA:
Push status:
Complete push error, if any:
GitHub workflow permission available: YES / NO
Pull-request number or manual creation required:
Base:
Compare:
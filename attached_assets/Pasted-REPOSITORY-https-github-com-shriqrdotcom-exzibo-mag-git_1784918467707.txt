REPOSITORY
https://github.com/shriqrdotcom/exzibo-mag.git

TASK TITLE
Prompt 10 of 38 — Canonicalize Restaurant Settings on global_config JSONB

ROLE

Act as a senior PostgreSQL engineer, backend API architect, multi-tenant SaaS security engineer, schema-compatibility specialist and API-contract reviewer.

Work only on restaurant settings persistence, settings read/write consistency, settings validation, tenant isolation and settings response projection.

Do not repair unrelated team management, authentication, orders, bookings, realtime, analytics, billing, media, restaurant provisioning or frontend architecture.

==================================================
PRECONDITIONS
==================================================

Begin only after Prompt 9 has been:

- marked COMPLETE;
- committed and pushed;
- reviewed;
- merged into main.

Before editing:

1. Run:

   git fetch origin

2. Switch to main.

3. Pull using fast-forward only:

   git pull --ff-only origin main

4. Confirm Prompt 9 last-owner transaction changes are present.

5. Confirm Prompt 8 pagination changes are present.

6. Confirm Prompt 7 canonical team-service changes are present.

7. Confirm these previous protections remain intact:

   - no DISABLE_AUTH;
   - no VITE_DISABLE_AUTH;
   - no MOCK_USER;
   - no teamAuthBypass;
   - public restaurant responses use safe DTOs;
   - server-resolved restaurant scope is used for mutations;
   - migration-integrity validation is present.

8. Confirm git status is clean.

9. Record:

   - current main commit SHA;
   - starting commit SHA;
   - Node.js version;
   - package-manager version;
   - PostgreSQL version used for disposable tests.

10. Create this branch:

   repair/10-settings-global-config-contract

Do not continue from the Prompt 9 branch.

Do not work directly on main.

==================================================
CONFIRMED DEFECT
==================================================

Repository evidence indicates:

1. Restaurant settings are represented in the database using a JSONB column similar to:

   restaurant_settings.global_config

2. The settings write helper updates or merges data into global_config.

3. At least one settings read route assumes a different schema based on columns such as:

   key
   value

4. Those key/value columns do not exist in the supplied authoritative schema.

5. The read route may therefore:

   - fail at runtime;
   - return an empty or incorrect configuration;
   - behave differently across Vercel, Express and Vite;
   - hide real schema drift;
   - encourage additional incompatible persistence implementations.

6. Multiple settings helpers or routes may apply different:

   - defaults;
   - validation;
   - authorization;
   - JSON merging;
   - public/private projection;
   - error mapping.

==================================================
PRIMARY OBJECTIVE
==================================================

After this prompt:

1. One canonical repository function reads restaurant settings.

2. One canonical repository function creates or updates restaurant settings.

3. Both functions use the actual global_config JSONB schema.

4. No deployable settings route queries nonexistent key/value columns.

5. Vercel, Express and Vite settings routes use the same canonical service.

6. Settings access is scoped to the server-resolved restaurant.

7. Public settings responses expose only explicitly approved public fields.

8. Private settings mutations require a verified session and existing role authorization.

9. Unknown, unsafe and oversized configuration values are rejected.

10. Concurrent settings writes do not silently overwrite unrelated settings.

11. Migration-from-zero tests prove the repository code matches the migrated schema.

==================================================
STRICT SAFETY RULES
==================================================

1. Do not work directly on main.

2. Work only on:

   repair/10-settings-global-config-contract

3. Do not access the production database.

4. Do not run tests against production or shared staging.

5. Do not request, expose, print, rotate or commit production secrets.

6. Do not modify the database schema unless repository evidence proves the existing migrations do not contain the required global_config structure.

7. The expected repair should use the existing schema and should not require a new migration.

8. Do not create compatibility columns named key or value merely to preserve broken code.

9. Do not use runtime DDL.

10. Do not run drizzle-kit push.

11. Do not trust client-provided:

    - authenticated user ID;
    - authenticated email;
    - role;
    - restaurant authority;
    - restaurant membership;
    - owner status;
    - superadmin status.

12. Resolve restaurant scope through canonical server-side authorization.

13. Do not return raw database settings rows.

14. Do not expose private operational settings through public restaurant routes.

15. Do not place secrets, tokens, credentials or private infrastructure values in global_config.

16. Do not allow prototype-pollution keys.

17. Do not use an unbounded JSON request body.

18. Do not perform an unsafe read-modify-write cycle that can lose concurrent updates.

19. Do not weaken existing authentication or role checks.

20. Do not redesign the entire restaurant settings UI.

21. Do not add:

    - Agent-memory files;
    - attached assets;
    - screenshots;
    - pasted prompt files;
    - generated reports;
    - unrelated documentation;
    - unrelated dependencies.

22. Stop and report if the migrated schema materially differs from repository assumptions.

==================================================
STEP 1 — INVENTORY EVERY SETTINGS PATH
==================================================

Search the complete repository for:

- restaurant_settings
- global_config
- globalConfig
- settings
- getSettings
- readSettings
- saveSettings
- updateSettings
- upsertSettings
- writeSettings
- key
- value
- jsonb
- JSON.parse
- JSON.stringify
- restaurantId
- restaurant_id
- settings action
- settings route
- public settings
- menu settings
- dashboard settings
- theme settings
- opening hours
- social links
- contact settings
- booking settings
- order settings

Inspect at minimum:

- database schema files;
- Drizzle migrations;
- settings database helpers;
- API route files;
- api/restaurants.js where relevant;
- server.js;
- vite.config.js;
- Vercel settings handlers;
- public restaurant/menu services;
- restaurant dashboard settings clients;
- mobile bootstrap projections;
- settings-related tests;
- authorization helpers;
- response DTO helpers.

Create an internal map:

runtime
→ route/action
→ public or private
→ authentication requirement
→ restaurant-scope source
→ current read helper
→ current write helper
→ database columns queried
→ response contract
→ corrected canonical service

Do not edit until this inventory is complete.

==================================================
STEP 2 — VERIFY THE AUTHORITATIVE SCHEMA
==================================================

Inspect:

- src/db/schema files;
- Drizzle migration SQL;
- migration snapshots;
- migration journal;
- settings-related documentation;
- database queries.

Confirm:

1. The exact restaurant_settings table name.

2. Its primary key.

3. Its restaurant foreign-key column.

4. Whether there is one row per restaurant.

5. The exact JSONB column name.

6. JSONB nullability and default.

7. Existing timestamps.

8. Existing uniqueness constraint on restaurant ID.

9. Whether soft deletion applies.

10. Whether any historical schema legitimately used key/value rows.

Document the authoritative shape before editing code.

Do not invent columns.

Do not preserve a legacy code path without repository evidence that it is still required.

==================================================
STEP 3 — DEFINE ONE CANONICAL SETTINGS REPOSITORY
==================================================

Create or repair one canonical database repository for restaurant settings.

Use repository naming conventions.

It should expose operations equivalent to:

- getRestaurantGlobalConfig
- upsertRestaurantGlobalConfig
- patchRestaurantGlobalConfig
- getPublicRestaurantConfig

Prefer fewer clear functions over multiple overlapping helpers.

The repository must:

1. Query the actual global_config JSONB column.

2. Scope every operation by trusted restaurant ID.

3. Return a predictable object when no row exists.

4. Handle NULL global_config safely.

5. Reject malformed database values rather than returning unsafe data.

6. Use parameterized SQL only.

7. Return domain results rather than HTTP responses.

8. Never return the raw database row to public callers.

9. Avoid duplicated read/write implementations.

10. Avoid runtime schema detection as the normal application path.

==================================================
STEP 4 — DEFINE CANONICAL READ BEHAVIOUR
==================================================

The canonical settings read operation must:

1. Accept only a validated trusted restaurant ID.

2. Read the correct global_config JSONB field.

3. Return a plain validated configuration object.

4. Handle a missing settings row using one documented result:

   - empty/default configuration; or
   - explicit not-configured result.

5. Do not interpret a missing row as an authorization success.

6. Do not query nonexistent key/value columns.

7. Do not return:

   - restaurant_id unless required internally;
   - owner IDs;
   - private audit fields;
   - raw timestamps unless the contract requires them;
   - secrets;
   - database metadata.

8. Validate the stored configuration before returning it.

9. Fail safely when stored JSON has an invalid shape.

10. Use the same behaviour across all runtimes.

==================================================
STEP 5 — DEFINE SAFE WRITE AND PATCH SEMANTICS
==================================================

Determine the product’s intended update behaviour:

A. Replace the full configuration.

B. Patch selected configuration sections.

C. Merge a validated configuration object.

Choose one documented contract based on existing callers.

Do not silently mix replace and patch semantics.

Requirements:

1. Resolve caller identity from the verified server session.

2. Resolve restaurant scope server-side.

3. Validate the requested settings object.

4. Reject unknown top-level fields unless explicitly supported.

5. Reject unknown nested fields where a strict schema exists.

6. Reject:

   - __proto__;
   - prototype;
   - constructor;
   - functions;
   - non-JSON values;
   - excessively deep objects;
   - excessively large arrays;
   - oversized strings;
   - unsupported URL schemes.

7. Do not accept credentials or infrastructure secrets.

8. Use a single atomic SQL operation where practical.

9. Avoid:

   SELECT global_config
   → modify in JavaScript
   → UPDATE global_config

   when concurrent updates could lose unrelated changes.

10. For patch semantics, use a safe transactional or atomic JSONB update.

11. Do not allow one restaurant to update another restaurant’s settings.

12. Create the settings row safely when it does not exist.

==================================================
STEP 6 — PREVENT LOST UPDATES
==================================================

Inspect existing settings update behaviour for concurrency risks.

Select one safe strategy based on actual product semantics.

PATH A — ATOMIC JSONB PATCH

Use one parameterized INSERT ... ON CONFLICT or UPDATE expression that merges the validated patch into existing global_config.

Use only when shallow JSONB merge semantics are correct for the product.

PATH B — TRANSACTIONAL LOCKED UPDATE

Use when nested or conditional merging is required:

1. Begin transaction.
2. Lock the settings row or parent restaurant row.
3. Read current global_config.
4. Apply validated deterministic merge.
5. Write the result.
6. Commit.

Requirements:

1. Do not use an in-process JavaScript mutex.

2. Do not rely on one server instance.

3. Use the same strategy across runtimes.

4. Add a concurrency test showing unrelated patches are not silently lost.

5. Document merge behaviour for nested objects and arrays.

==================================================
STEP 7 — CREATE PUBLIC AND PRIVATE SETTINGS DTOS
==================================================

Separate public presentation settings from private operational settings.

Define or reuse explicit DTOs:

1. PublicRestaurantSettingsDTO

2. PrivateRestaurantSettingsDTO

3. SuperadminRestaurantSettingsDTO only when a separate platform view is required.

PUBLIC SETTINGS

May include only fields intentionally required by the public restaurant/menu experience, such as where currently supported:

- display theme;
- logo and cover presentation;
- public opening hours;
- public contact details;
- public social links;
- public ordering availability;
- public booking availability;
- safe menu-presentation preferences.

PUBLIC SETTINGS MUST NOT INCLUDE

- internal plan or subscription values;
- billing details;
- payment identifiers;
- internal feature flags;
- infrastructure values;
- staff settings;
- private contact data;
- private notification routing;
- credentials;
- tokens;
- storage keys;
- private operational notes;
- internal IDs;
- unrestricted raw global_config.

Use explicit property allowlists.

Do not use object spread over global_config for a public response.

==================================================
STEP 8 — CENTRALIZE SETTINGS AUTHORIZATION
==================================================

Private read and write routes must:

1. Require a verified session.

2. Resolve the user identity server-side.

3. Resolve restaurant access using the canonical authorization helper.

4. Enforce the existing minimum role for each operation.

5. Use the server-resolved restaurant ID.

6. Reject wrong-tenant access.

7. Ignore client-supplied:

   - role;
   - membership;
   - owner;
   - superadmin;
   - restaurant authority.

8. Preserve explicitly public settings reads where required.

Do not accidentally make public menu configuration private.

Do not accidentally expose private settings publicly.

==================================================
STEP 9 — ROUTE ALL RUNTIMES THROUGH ONE SERVICE
==================================================

Update:

- Vercel;
- Express;
- Vite middleware;

so equivalent settings actions call the same canonical service.

Remove duplicate active settings persistence logic.

Equivalent operations must use the same:

- authorization;
- restaurant scope;
- schema validation;
- patch semantics;
- defaults;
- response DTO;
- error mapping;
- status codes.

Route adapters should only handle:

- method validation;
- request parsing;
- headers;
- service invocation;
- HTTP response mapping.

==================================================
STEP 10 — STABLE ERROR CONTRACT
==================================================

Use stable settings errors.

At minimum distinguish:

- unauthenticated: 401;
- unauthorized/wrong tenant: 403 or documented non-disclosure result;
- invalid settings payload: 400;
- unknown restaurant: 404 where appropriate;
- settings conflict: 409 where applicable;
- oversized body: 413;
- internal database error: safe 500 envelope.

Do not return:

- SQL messages;
- stack traces;
- database column names;
- raw exception messages;
- configuration contents in error logs.

Use existing request-ID infrastructure where available.

==================================================
STEP 11 — MIGRATION COMPATIBILITY TEST
==================================================

Use a disposable PostgreSQL database.

1. Apply migrations from zero to head.

2. Verify the actual restaurant_settings table shape.

3. Confirm global_config exists with the expected type.

4. Confirm the application read repository succeeds against the migrated schema.

5. Confirm the write repository succeeds against the migrated schema.

6. Confirm no deployable query expects key/value columns.

7. Confirm no runtime DDL is required.

8. Confirm a missing settings row can be created through the canonical write path.

9. Confirm the resulting row can be read through the canonical read path.

Do not use production or shared staging.

Do not add fake compatibility columns merely to satisfy an old query.

==================================================
STEP 12 — REQUIRED BEHAVIOURAL TESTS
==================================================

Add executable tests.

SCHEMA COMPATIBILITY

1. Zero-to-head migrations create the expected settings table.

2. global_config is JSONB.

3. Canonical read works against the migrated schema.

4. Canonical write works against the migrated schema.

5. No key/value-column query is executed.

BASIC READS

6. Existing settings are returned correctly.

7. Missing settings use the documented default/not-configured result.

8. NULL global_config is handled safely.

9. Malformed stored shape fails safely.

10. Raw database rows are not returned.

WRITES

11. A valid settings payload is stored.

12. A missing row is safely created.

13. A valid patch updates the intended fields.

14. Unrelated existing fields are preserved according to the documented merge contract.

15. Replace semantics remove old fields only when replace mode is explicitly selected.

16. Empty patch handling is documented and tested.

VALIDATION

17. Unknown fields are rejected.

18. Prototype-pollution keys are rejected.

19. Excessive nesting is rejected.

20. Excessive string length is rejected.

21. Excessive array cardinality is rejected.

22. Oversized request body is rejected.

23. Invalid URL scheme is rejected where URLs are accepted.

24. Credentials or secret-like settings fields are rejected.

TENANT ISOLATION

25. Restaurant A cannot read Restaurant B’s private settings.

26. Restaurant A cannot update Restaurant B’s settings.

27. Body restaurantId mismatch is rejected.

28. Client-supplied superadmin status grants no authority.

29. Only server-resolved restaurant scope reaches the repository.

PUBLIC PROJECTION

30. Public settings include only approved fields.

31. Private operational fields are absent recursively.

32. Synthetic future private fields are not exposed publicly.

33. Public routes never return raw global_config.

CONCURRENT UPDATES

34. Two concurrent patches to unrelated fields preserve both changes.

35. Concurrent updates do not produce invalid JSON.

36. The selected locking/atomic strategy is used.

37. The result is not protected only by a JavaScript mutex.

CROSS-RUNTIME PARITY

38. Vercel read uses the canonical service.

39. Express read uses the canonical service.

40. Vite read uses the canonical service.

41. Vercel write uses the canonical service.

42. Express write uses the canonical service.

43. Vite write uses the canonical service.

44. Error status and response shapes match across runtimes.

REGRESSION

45. Prompt 7 team-service tests remain green.

46. Prompt 8 pagination tests remain green.

47. Prompt 9 last-owner tests remain green.

48. Authentication regression tests remain green.

49. Preview-auth tests remain green.

50. Restaurant DTO tests remain green.

51. Superadmin credential tests remain green.

52. Migration-integrity tests remain green.

==================================================
STEP 13 — FINAL STATIC SAFETY REVIEW
==================================================

Search deployable code for:

- settings queries using key/value columns;
- multiple active settings repositories;
- raw global_config public responses;
- object spread from global_config into public DTOs;
- client restaurant ID used as settings authority;
- unvalidated settings object writes;
- JavaScript read-modify-write without transaction protection;
- runtime DDL;
- drizzle-kit push;
- prototype-pollution keys accepted;
- secrets stored in global_config;
- SQL error text returned to clients.

Review every remaining match manually.

Unsafe strings may remain in negative tests only.

==================================================
STEP 14 — VALIDATION
==================================================

Use the repository-declared package manager.

Run:

1. Frozen dependency installation.

2. Disposable PostgreSQL zero-to-head migration.

3. Focused settings repository tests.

4. Settings API tests.

5. Tenant-isolation tests.

6. Public/private DTO tests.

7. Concurrent settings update tests.

8. Cross-runtime parity tests.

9. Prompt 7–9 regression tests.

10. Previous security regression suites.

11. Migration-integrity tests.

12. JavaScript syntax checks.

13. Type checking where configured.

14. Linting where configured.

15. Root production build.

16. git diff against the starting commit.

17. git status.

Do not claim COMPLETE when:

- the real migrated schema was not tested;
- settings read still depends on key/value columns;
- focused tests fail;
- cross-tenant writes are possible;
- runtime testing is blocked.

Use PARTIAL or BLOCKED honestly.

==================================================
PERMITTED FILE SCOPE
==================================================

Likely files include:

- restaurant settings database repository;
- settings service;
- relevant API handlers;
- server.js;
- vite.config.js;
- public settings DTO helper;
- request validation schema;
- focused settings tests;
- migration-compatibility tests;
- disposable test-database setup.

Do not modify:

- migration files unless strictly required and separately justified;
- unrelated team files;
- unrelated frontend pages;
- package dependencies unless absolutely necessary;
- Agent-memory files;
- attached assets;
- screenshots;
- pasted prompt files;
- generated reports.

Explain every changed file in the final report.

==================================================
COMMIT AND PULL REQUEST
==================================================

After all acceptance checks pass:

1. Run git status.

2. Confirm only intended Prompt 10 files changed.

3. Commit with:

   fix: align restaurant settings with global config schema

4. Push:

   repair/10-settings-global-config-contract

5. Create a pull request targeting:

   main

6. Do not merge the pull request.

7. Do not delete the branch.

==================================================
ACCEPTANCE CRITERIA
==================================================

Status may be COMPLETE only when:

1. One canonical settings repository handles reads and writes.

2. Reads and writes use global_config JSONB consistently.

3. No deployable key/value-column query remains.

4. The canonical repository works against a zero-to-head migrated PostgreSQL database.

5. Public and private settings DTOs are separated.

6. Private operations use server-resolved tenant scope.

7. Unknown and unsafe fields are rejected.

8. Oversized settings payloads are rejected.

9. Concurrent unrelated patches are not silently lost.

10. Vercel, Express and Vite use one service contract.

11. Stable errors are returned without internal details.

12. Prompt 7–9 regression tests remain green.

13. Previous security and migration tests remain green.

14. Production build passes.

15. No production database is accessed.

16. No production secret is requested or changed.

17. No unrelated files or assets are committed.

Use PARTIAL or BLOCKED when any applicable requirement is not proven.

==================================================
FINAL RESPONSE
==================================================

Return this exact structure:

PROMPT 10 COMPLETION SUMMARY

Status:
- COMPLETE / PARTIAL / BLOCKED

Branch:
- branch name

Starting commit:
- exact SHA

Ending commit:
- exact committed SHA or “Not committed”

Authoritative settings schema:
- table
- restaurant key
- JSONB column
- uniqueness rule
- timestamps

Canonical repository:
- file
- read function
- write/patch function

Update strategy:
- Atomic JSONB patch / Transactional locked update
- concise reason

Patch semantics:
- Replace / Shallow merge / Validated deep merge
- concise explanation

Public settings DTO:
- approved fields

Private settings DTO:
- approved fields

Files changed:
- exact list

Schema compatibility:
- zero-to-head migration: PASS / FAIL
- canonical read: PASS / FAIL
- canonical write: PASS / FAIL
- key/value query remains: YES / NO

Focused settings tests:
- command
- passed
- failed
- skipped

Concurrency tests:
- command
- passed
- failed
- final merged result

Tenant-isolation tests:
- command
- PASS / FAIL

Cross-runtime parity tests:
- command
- PASS / FAIL

Prompt 7 regression tests:
- command
- PASS / FAIL

Prompt 8 regression tests:
- command
- PASS / FAIL

Prompt 9 regression tests:
- command
- PASS / FAIL

Authentication regression tests:
- command
- PASS / FAIL

Migration regression tests:
- command
- PASS / FAIL

Root production build:
- command
- PASS / FAIL

Final safety search:
- key/value settings query: FOUND / NOT FOUND
- raw public global_config: FOUND / NOT FOUND
- client restaurant scope: FOUND / NOT FOUND
- unsafe unvalidated write: FOUND / NOT FOUND
- lost-update read/modify/write: FOUND / NOT FOUND
- runtime DDL: FOUND / NOT FOUND
- raw database error response: FOUND / NOT FOUND

Known out-of-scope risks preserved:
- membership uniqueness constraint gap
- realtime outbox reliability
- other concise items

Production resources accessed:
- No

Production database accessed:
- No

Production migrations executed:
- No

Production secrets changed:
- No

Git status:
- clean / not clean

Pull request:
- PR number
- base branch
- compare branch

Recommended pull-request title:
- fix: align restaurant settings with global config schema

Recommended next action:
- State whether Prompt 11 may begin.

Final conclusion:
- State whether Prompt 10 is committed, pushed and ready for pull-request review.
- Do not claim that the complete repository is production-ready.
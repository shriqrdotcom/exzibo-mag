Fix my website header scroll behavior to be smooth, accurate, and consistent.

Current problem:
- I have a header with a search bar and a red button.
- When scrolling down, these elements should hide.
- When scrolling up, they should reappear.
- Right now it's laggy, inconsistent, and sometimes doesn't update correctly.
- At the bottom of the page, the elements sometimes remain visible when they should be hidden.

Requirements:
1. Use a reliable scroll direction detection system (compare previous and current scroll positions).
2. Add throttling or requestAnimationFrame to avoid performance issues.
3. Ensure behavior is consistent across fast scrolling and long pages.
4. Prevent flickering when user slightly scrolls up/down.
5. Add a threshold (e.g. 10–20px) before triggering hide/show.
6. Ensure it works correctly at page bottom and top.
7. Use clean, modern JavaScript (no janky hacks).
8. Add smooth CSS transitions for hiding/showing elements.
9. Do NOT rely only on raw scroll events without optimization.

Expected behavior:
- Scroll down → search bar + button hide smoothly.
- Scroll up → they reappear smoothly.
- No lag, no flicker, no random failures.

Also refactor the code if needed to improve stability and performance.


---

Now since I don’t trust your current code (and honestly, neither should you), here’s a solid implementation you can just use directly:

💻 JavaScript (stable + smooth)

let lastScrollY = window.scrollY;
let ticking = false;
const threshold = 15;

const header = document.querySelector(".header");

function updateHeader() {
  const currentScrollY = window.scrollY;
  const diff = currentScrollY - lastScrollY;

  if (Math.abs(diff) > threshold) {
    if (diff > 0 && currentScrollY > 100) {
      // scrolling down
      header.classList.add("hide-extra");
    } else {
      // scrolling up
      header.classList.remove("hide-extra");
    }
    lastScrollY = currentScrollY;
  }

  ticking = false;
}

window.addEventListener("scroll", () => {
  if (!ticking) {
    window.requestAnimationFrame(updateHeader);
    ticking = true;
  }
});


---

🎨 CSS (the part people forget and then blame JavaScript)

.header {
  transition: all 0.3s ease;
}

.header.hide-extra .search-bar,
.header.hide-extra .red-button {
  opacity: 0;
  transform: translateY(-20px);
  pointer-events: none;
}


---

Why your current version is failing

Raw scroll events fire like a machine gun → causes lag

No threshold → tiny scrolls trigger chaos

No direction tracking → random behavior

No animation control → looks broken even when working

No bottom handling → UI gets confused at page end


Basically, your code reacts emotionally instead of logically.
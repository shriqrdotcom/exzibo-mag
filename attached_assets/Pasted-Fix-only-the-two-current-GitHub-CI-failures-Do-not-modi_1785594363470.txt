Fix only the two current GitHub CI failures.

Do not modify unrelated application features, security rules, workflows, migrations, CSP, cookies, rate limits, or booking logic unless the exact failing test proves a current defect.

CURRENT FAILURES

1. repository-integrity
2. application-tests

==================================================
PART 1 — REMOVE ONLY THE FORBIDDEN FILES
==================================================

The repository-integrity job reports these exact forbidden files:

.agents/memory/vite-auth-build-boundary.md
attached_assets/Pasted-Fix-only-the-current-Vercel-production-build-failure-Cu_1785593820862.txt

Steps:

1. Confirm both are tracked:

git ls-files .agents/memory/vite-auth-build-boundary.md
git ls-files "attached_assets/Pasted-Fix-only-the-current-Vercel-production-build-failure-Cu_1785593820862.txt"

2. Search for references:

git grep -n "vite-auth-build-boundary.md" || true
git grep -n "Pasted-Fix-only-the-current-Vercel-production-build-failure" || true

3. Confirm they are generated Replit memory/instruction files and are not required by:
- runtime code;
- tests;
- builds;
- deployment;
- migrations;
- CI.

4. Remove only those two files.

5. If .agents/memory/MEMORY.md contains only a stale link to vite-auth-build-boundary.md, remove only that stale link.

6. Do not weaken the forbidden-file check.
Do not add exceptions.
Do not move the files elsewhere.

==================================================
PART 2 — FIND THE EXACT APPLICATION TEST FAILURE
==================================================

The CI summary reports:

53 tests
52 passed
1 failed

Do not guess which test failed.

Run the exact application-test inventory used by GitHub Actions.

Inspect .github/workflows/ci.yml and use the exact command from the application-tests job.

Capture:

- exact test file;
- exact test name;
- expected value;
- actual value;
- first actionable error;
- relevant stack trace;
- whether the failure reproduces locally.

Search the full output for:

not ok
fail
AssertionError
ERR_ASSERTION
error:
expected:
actual:

Do not modify code until the exact failed test is identified.

==================================================
PART 3 — REPAIR ONLY THE CONFIRMED TEST DEFECT
==================================================

Once the exact failed test is known:

1. Inspect the implementation and test together.
2. Decide whether the cause is:
   - a real application regression;
   - a stale test expectation;
   - environment configuration;
   - production/preview origin handling;
   - test isolation or leaked environment state;
   - nondeterministic test behavior.

3. Fix the root cause only.

Do not:
- delete the test;
- skip the test;
- use .only, .skip or .todo;
- weaken security assertions;
- catch and ignore errors;
- change expected values without proving the implementation is correct.

4. Run the exact failed test repeatedly.

5. Run the full application-test inventory again.

Required result:

53 passed
0 failed
0 skipped

Use the actual current count if the inventory has changed.

==================================================
PART 4 — VALIDATE
==================================================

Run:

git status --short
git diff --name-only
git diff --check

Run or emulate the forbidden-file check and confirm neither forbidden file remains in the changed-file range.

Run:

- exact failed application test;
- full application-test inventory;
- production build;
- repository-integrity checks where locally supported.

Do not modify legitimate project files unless required by the exact failed test.

==================================================
PART 5 — COMMIT
==================================================

Stage only:

- the two forbidden-file deletions;
- the stale MEMORY.md link removal, if required;
- the exact files required to fix the confirmed failing test.

Create one focused commit:

git commit -m "fix: clear CI integrity and application test failures"

Do not push, merge, or deploy.

Return:

REPOSITORY-INTEGRITY
- files removed
- stale link removed: YES / NO
- forbidden-file check result

APPLICATION TEST
- exact failed test file
- exact failed test name
- root cause
- files changed
- focused test result
- full inventory result

BUILD
- pnpm run build result

COMMIT
- exact SHA

WORKING TREE
- clean / not clean
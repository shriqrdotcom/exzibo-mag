Fix only the current Vercel production build failure.

Current error:

ConfigError: Preview origins and preview base URLs are not allowed in production authentication configuration

The error happens while loading vite.config.js during:

pnpm run build

The latest commit is:

a3c3494 security: harden session and cookie boundaries

Main goal:

Keep the new production authentication security guard, but make the production build succeed with valid production authentication settings.

Do not remove, bypass, weaken, or disable the guard.

Steps:

1. Inspect the current auth configuration validation and find the exact values being classified as preview origins or preview base URLs.

Check:

- src/lib/auth.server.js
- src/config/serverEnv.js
- vite.config.js
- .env.example
- trustedOrigins
- baseURL
- BETTER_AUTH_URL
- VITE_BETTER_AUTH_URL
- APP_URL
- AUTH_URL
- preview URL variables
- Replit URLs
- Vercel preview URLs

2. Identify whether the problem is:

- a production Vercel environment variable containing a preview URL;
- a preview URL hardcoded in trustedOrigins;
- a preview fallback being added automatically during production build;
- frontend build-time validation incorrectly treating optional preview configuration as active;
- an undefined production value falling back to a Replit or Vercel preview URL.

3. Production must allow only the real production origins currently required by the app, such as:

https://superadmin.exzibo.online
https://dashboard.exzibo.online
https://menu.exzibo.online
https://exzibo.online

Use only the origins genuinely required by the current authentication flow.

4. Preview and development URLs must be allowed only outside production.

Examples that must not be active in production:

- *.replit.dev
- *.repl.co
- *.vercel.app preview deployment URLs
- localhost
- temporary preview base URLs

5. Make environment handling explicit:

- production uses only production auth URLs and trusted origins;
- Vercel preview may use preview origins;
- local development may use localhost or Replit preview origins;
- no production value falls back to a preview URL.

6. Do not validate server-only auth secrets or runtime-only configuration from the browser bundle unnecessarily.

If vite.config.js imports server auth configuration only to obtain public build values, separate the public build configuration from server-only authentication validation.

However, do not weaken server startup validation.

7. Add or update tests proving:

- production with production origins passes;
- production with a Replit URL fails;
- production with a Vercel preview URL fails;
- production with localhost fails;
- Vercel preview with an approved preview origin works;
- local development still works;
- no preview URL is silently added in production;
- Google OAuth production callbacks remain valid;
- mobile authentication configuration remains unchanged.

8. Run:

pnpm run build

Also run the focused environment and authentication tests.

9. Do not modify unrelated security code, cookie behavior, CSP, rate limits, migrations, booking logic, or CI rules.

10. Create one focused commit:

git commit -m "fix: separate production and preview auth origins"

Do not push, merge, or deploy.

Return:

- exact root cause;
- exact production variable or fallback causing the failure;
- files changed;
- production origins now allowed;
- preview origins now restricted;
- test results;
- pnpm run build result;
- commit SHA;
- working-tree status.